import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.robertgannard.arscanner',
  appName: 'AR Book Scanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
